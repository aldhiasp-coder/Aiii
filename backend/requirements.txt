# Dipakai langsung oleh api/index.py dan app/* -- wajib untuk deploy ini.
numpy>=1.26
pandas>=2.0
requests>=2.31
fastapi>=0.110
uvicorn[standard]>=0.29

# Belum dipakai kode manapun saat ini -- tambahkan lagi saat benar-benar
# mengimplementasikan fitur terkait, supaya bundle & build tetap ringan
# (spek poin 43 -- PERFORMANCE):
# pydantic>=2.6           # jika dataclass manual di app/domain diganti
# redis>=5.0               # Phase berikutnya: RedisCache production
# sqlalchemy>=2.0          # Phase berikutnya: persistence (watchlist, alerts)
# alembic>=1.13            # DB migrations
# celery>=5.3               # Phase News/Economic Calendar: scheduler
# pytest>=8.0               # test runner alternatif unittest
# pytest-asyncio>=0.23
# httpx>=0.27                # async client + testing FastAPI
